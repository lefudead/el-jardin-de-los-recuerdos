/**
 * PreloadScene (GDD técnico §49).
 * MVP: no hay assets binarios externos (se usan emojis y audio sintetizado).
 * En un futuro Phaser se cargarían sprites/audio/música aquí.
 */
export function preload() {
  return Promise.resolve();
}
